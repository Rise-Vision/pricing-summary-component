{
  "name": "pricing-summary-component",
  "description": "Shows monthly or yearly cost summary",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint pricing-summary-component.mjs",
    "pretest-ci": "npm run lint",
    "test-ci": "wdio wdio.conf.js",
    "prebuild": "mkdir -p build && cp pricing-summary-component-test.html build",
    "build": "terser pricing-summary-component.mjs --compesss --mangle --output build/pricing-summary-component.mjs"
  },
  "devDependencies": {
    "acorn": "^6.1.1",
    "@wdio/cli": "^5.9.1",
    "@wdio/dot-reporter": "^5.7.8",
    "@wdio/local-runner": "^5.9.1",
    "@wdio/mocha-framework": "^5.9.1",
    "@wdio/sync": "^5.9.1",
    "eslint": "^5.16.0",
    "terser": "^4.0.0"
  }
}
